{
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "graphql/schema/*.graphql"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }]
}
