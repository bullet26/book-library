# Series data type
type Series {
  id: ID!
  authorID: ID!
  title: String!
  booksInSeries: [Book]
}

# input data for add series
input SerieInput {
  authorID: ID!
  title: String!
}

extend type Mutation {
  # Add serie
  createSerie(input: SerieInput!): Series
}
